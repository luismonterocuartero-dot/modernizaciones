spring:
  application:
    name: svehi-backend
  datasource:
    url: ${SUPABASE_URL:jdbc:postgresql://aws-1-eu-central-1.pooler.supabase.com:5432/postgres}
    username: ${SUPABASE_USER:postgres.hxojihuhvvyumbrcwdwv}
    password: ${SUPABASE_PASSWORD:Pr0q50qF3888}
    driverClassName: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
#    h2:
#      console:
#        enabled: true
#        path: /h2-console

server:
  port: 8080
  error:
    include-message: always

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  tracing:
    enabled: true
    sampling:
      probability: 1.0

logging:
  level:
    root: INFO
    es.juntadeandalucia.cpai.svehi: DEBUG
    org.springframework.web: INFO
