<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security-4.2.xsd">

  	 <http pattern="/resources/**" security="none" />
  	 
  	 <http auto-config="true" use-expressions="true">
        <!-- Protección CSRF -->
        <csrf disabled="true"/>
        <!-- Configuración del login -->
        <form-login login-page="/" login-processing-url="/ReturnAuthenticationServlet"/>

        <!-- URL sin protección -->
        <intercept-url pattern="/" access="permitAll"/>
        <intercept-url pattern="/login" access="permitAll"/>
        <intercept-url pattern="/logout" access="permitAll"/>
        <intercept-url pattern="/CallAuthenticationServlet" access="permitAll"/>
        <intercept-url pattern="/ReturnAuthenticationServlet" access="permitAll"/>
        <intercept-url pattern="/ReturnAFirmaServlet" access="permitAll"/>

        <!-- El resto de la aplicación está protegida -->
        <intercept-url pattern="/vehiculos" access="isAuthenticated()" />
        <intercept-url pattern="/talleres" access="isAuthenticated()" />
	<intercept-url pattern="/companias" access="isAuthenticated()" />
	<intercept-url pattern="/conceptos" access="isAuthenticated()" />
        <intercept-url pattern="/marcas" access="isAuthenticated()" />
        <intercept-url pattern="/modelos" access="isAuthenticated()" />
        <intercept-url pattern="/parametros" access="isAuthenticated()" />
        <intercept-url pattern="/perfiles" access="isAuthenticated()" />
        <intercept-url pattern="/permisos" access="isAuthenticated()" />
        <intercept-url pattern="/usuarios" access="isAuthenticated()" />
        <intercept-url pattern="/syncroDoc" access="isAuthenticated()" />
        <intercept-url pattern="/**" access="isAuthenticated()"/>

    </http>
    
    <authentication-manager alias="authenticationManager">
		<authentication-provider user-service-ref='userDetailsService'/>
	</authentication-manager>
	
	 <beans:bean id="userDetailsService" class="es.juntadeandalucia.cpai.svehi.service.UserDetailsServiceImpl"/>

</beans:beans>